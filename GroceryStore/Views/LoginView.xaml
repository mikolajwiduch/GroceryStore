<Window x:Class="GroceryStore.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="400">
    <Grid>
        <StackPanel>
            <TextBox Width="200" Margin="10" PlaceholderText="Username" Text="{Binding Username}" />
            <PasswordBox Width="200" Margin="10" Password="{Binding Password}" />
            <Button Content="Login" Width="100" Margin="10" Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</Window>
